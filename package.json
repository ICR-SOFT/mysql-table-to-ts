{
  "name": "mttots",
  "description": "This CLI tool generates TypeScript classes based on the structure of a MySQL table. The resulting classes include information about primary keys, indexes, and foreign keys, allowing you to leverage this information in your TypeScript project.",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "start": "tsc -p . && node dist/index.js"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "knex": "^2.5.1",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
